
pipeline {

    agent any

    stages {
        stage("Stage 1") {
            steps {
                echo "Welcome "
                echo 'Pulling...' + env.BRANCH_NAME
            }
        }
    }
}