
pipeline {

    agent any

    stages {
        stage("Stage 1") {
            steps{
                echo "My first pipeline 1"
                echo "My first pipeline 3"
            }
        }
    }
}